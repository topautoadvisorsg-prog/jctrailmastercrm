{
  "framework": "vite",
  "installCommand": "corepack yarn install --immutable",
  "buildCommand": "corepack yarn nx build twenty-front && node scripts/write-vercel-front-env.mjs",
  "outputDirectory": "packages/twenty-front/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
