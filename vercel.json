{
  "framework": "vite",
  "installCommand": "corepack yarn install --immutable",
  "buildCommand": "cd packages/twenty-front && NODE_ENV=production NODE_OPTIONS=--max-old-space-size=6144 npx vite build && cd ../.. && node scripts/write-vercel-front-env.mjs",
  "outputDirectory": "packages/twenty-front/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
